services:
  node:
    image: node:24
    user: node
    working_dir: /home/node/app
    environment:
      - NODE_ENV=test
    volumes:
      - /mnt/nas_pool/nodejs/web_test_nodejs:/home/node/app
    ports:
      # use if it is necessary to expose the container to the host machine
      - 8890:8888
    command:
      - npm
      - start
